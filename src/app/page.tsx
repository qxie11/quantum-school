import { Header } from "@features/index";
import { HomePage } from "@pages/index";

const Home = () => {
  return (
    <main>
      <Header />
      <HomePage />
    </main>
  );
};

export default Home;
